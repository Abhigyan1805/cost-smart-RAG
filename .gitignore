# Local build / data artifacts (never committed)
# (index artifacts under data/index/ are reproducible via the single index build command)
data/
telemetry/
*.parquet
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
